const greetPerson = (firstName, lastName) => {
 return `Hi, ${firstName} ${lasttName}`;
}

const greetPerson = (firstName, lastName) => `Hi, ${firstName} ${lasttName}`;

console.log(greetPerson('Richard', 'Lee'));